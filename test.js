for (var i=0; var<60; var++) {
  ellipseMode(CORNER);
  angleMode(DEGREES);
  noStroke();
  fill(clrBtnColorR*i, clrBtnColorG*i, clrBtnColorB*i);
  rotate(i*(PI/30);
  ellipse(mouseX, mouseZ, weight, weight/5);
}
